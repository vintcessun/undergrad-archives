# 第八课预习报告

## checking.c

- 作用: 验证输入是否合法并计算指定整数区间的平方和
- 功能: 
  程序首先要求用户输入一个区间的下限与上限（范围从 -10000000 到 +10000000），
  验证输入是否为整数且上下限是否合法，然后计算区间内各整数平方的总和并输出结果。

- 演示效果:
```c
// checking.c -- validating input
#include <stdio.h>
#include <stdbool.h>
// validate that input is an integer
long get_long(void);
// validate that range limits are valid
bool bad_limits(long begin, long end,
                long low, long high);
// calculate the sum of the squares of the integers
// a through b
double sum_squares(long a, long b);
int main(void)
{
    const long MIN = -10000000L;  // lower limit to range
    const long MAX = +10000000L;  // upper limit to range
    long start;                   // start of range
    long stop;                    // end of range
    double answer;
    
    printf("This program computes the sum of the squares of "
           "integers in a range.\nThe lower bound should not "
           "be less than -10000000 and\nthe upper bound "
           "should not be more than +10000000.\nEnter the "
           "limits (enter 0 for both limits to quit):\n"
           "lower limit: ");
    start = get_long();
    printf("upper limit: ");
    stop = get_long();
    while (start !=0 || stop != 0)
    {
        if (bad_limits(start, stop, MIN, MAX))
            printf("Please try again.\n");
        else
        {
            answer = sum_squares(start, stop);
            printf("The sum of the squares of the integers ");
            printf("from %ld to %ld is %g\n",
                    start, stop, answer);
        }
        printf("Enter the limits (enter 0 for both "
               "limits to quit):\n");
        printf("lower limit: ");
        start = get_long();
        printf("upper limit: ");
        stop = get_long();
    }
    printf("Done.\n");
    
    return 0;
}

long get_long(void)
{
    long input;
    char ch;
    
    while (scanf("%ld", &input) != 1)
    {
        while ((ch = getchar()) != '\n')
            putchar(ch);  // dispose of bad input
        printf(" is not an integer.\nPlease enter an ");
        printf("integer value, such as 25, -178, or 3: ");
    }
    
    return input;
}

double sum_squares(long a, long b)
{
    double total = 0;
    long i;
    
    for (i = a; i <= b; i++)
        total += (double)i * (double)i;
    
    return total;
}

bool bad_limits(long begin, long end,
                long low, long high)
{
    bool not_good = false;
    
    if (begin > end)
    {
        printf("%ld isn't smaller than %ld.\n", begin, end);
        not_good = true;
    }
    if (begin < low || end < low)
    {
        printf("Values must be %ld or greater.\n", low);
        not_good = true;
    }
    if (begin > high || end > high)
    {
        printf("Values must be %ld or less.\n", high);
        not_good = true;
    }
    
    return not_good;
}
```

```bash
gcc checking.c -o checking && echo "-3`n3`n0`n0" | ./checking
This program computes the sum of the squares of integers in a range.
The lower bound should not be less than -10000000 and
the upper bound should not be more than +10000000.
Enter the limits (enter 0 for both limits to quit):
lower limit: upper limit: The sum of the squares of the integers from -3 to 3 is 28
Enter the limits (enter 0 for both limits to quit):
lower limit: upper limit: Done.
```

## echo_eof.c

- 作用: 将标准输入的内容原样输出到标准输出直到遇到文件结束符（EOF）
- 功能: 
  程序读取输入的每个字符并立即输出，常用于测试输入输出流或作为数据透传工具。

- 演示效果:
```c
/* echo_eof.c -- repeats input to end of file */
#include <stdio.h>
int main(void)
{
    int ch;
    
    while ((ch = getchar()) != EOF)
        putchar(ch);
    
    return 0;
}
```

```bash
gcc echo_eof.c -o echo_eof && echo "Hello World!" | ./echo_eof
Hello World!
```

## echo.c

- 作用: 将输入的字符逐一输出，直到遇到字符 '#' 停止程序。
- 功能: 
  程序通过 `getchar()` 从标准输入流读取字符，直到检测到终止字符 `#` 为止，将之前的所有字符通过 `putchar()` 输出到标准输出。

- 演示效果:
```c
/* echo.c -- repeats input */
#include <stdio.h>
int main(void)
{
    char ch;
    
    while ((ch = getchar()) != '#')
        putchar(ch);
    
    return 0;
}
```

```bash
gcc echo.c -o echo && echo "Hello#World" | ./echo
Hello
```

## file_eof.c

- 作用: 从指定文件中读取并显示内容到标准输出。
- 功能: 
  程序请求用户输入文件名，尝试读取并输出该文件的全部内容。如果文件无法打开会给出提示并退出。

- 演示效果:
```c
// file_eof.c --open a file and display it
#include <stdio.h>
#include <stdlib.h>  // for exit()
int main()
{
    int ch;
    FILE * fp;
    char fname[50];         // to hold the file name
    
    printf("Enter the name of the file: ");
    scanf("%s", fname);
    fp = fopen(fname, "r"); // open file for reading
    if (fp == NULL)         // attempt failed
    {
	    printf("Failed to open file. Bye\n");
	    exit(1);            // quit program
    }
    // getc(fp) gets a character from the open file
    while ((ch = getc(fp)) != EOF)
        putchar(ch);
    fclose(fp);             // close the file
    
    return 0;
}
```

```bash
echo "Hello File" > test.txt && gcc file_eof.c -o file_eof && echo "test.txt" | ./file_eof
Enter the name of the file: Hello File
```

## guess.c

- 作用: 通过不断尝试从 1 到 100 的整数来猜测用户的数字。
- 功能: 
  使用简单的线性递增方式，每次询问用户当前猜测的数字是否正确，直到用户输入 'y' 结束。用户输入 'n' 表示错误，程序会继续增加猜测值并重复提问。

- 演示效果:
```c
/* guess.c -- an inefficient and faulty number-guesser */
#include <stdio.h>
int main(void)
{
    int guess = 1;
    
    printf("Pick an integer from 1 to 100. I will try to guess ");
    printf("it.\nRespond with a y if my guess is right and with");
    printf("\nan n if it is wrong.\n");
    printf("Uh...is your number %d?\n", guess);
    while (getchar() != 'y')      /* get response, compare to y */
        printf("Well, then, is it %d?\n", ++guess);
    printf("I knew I could do it!\n");
    
    return 0;
}
```

```bash
gcc guess.c -o guess && echo "n`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`nn`ny" | ./guess
Pick an integer from 1 to 100. I will try to guess it.
Respond with a y if my guess is right and with
an n if it is wrong.
Uh...is your number 1?
Well, then, is it 2?
Well, then, is it 3?
Well, then, is it 4?
Well, then, is it 5?
Well, then, is it 6?
Well, then, is it 7?
Well, then, is it 8?
Well, then, is it 9?
Well, then, is it 10?
Well, then, is it 11?
Well, then, is it 12?
Well, then, is it 13?
Well, then, is it 14?
Well, then, is it 15?
Well, then, is it 16?
Well, then, is it 17?
Well, then, is it 18?
Well, then, is it 19?
Well, then, is it 20?
Well, then, is it 21?
Well, then, is it 22?
Well, then, is it 23?
Well, then, is it 24?
Well, then, is it 25?
Well, then, is it 26?
Well, then, is it 27?
Well, then, is it 28?
Well, then, is it 29?
Well, then, is it 30?
Well, then, is it 31?
Well, then, is it 32?
Well, then, is it 33?
Well, then, is it 34?
Well, then, is it 35?
Well, then, is it 36?
Well, then, is it 37?
Well, then, is it 38?
Well, then, is it 39?
Well, then, is it 40?
Well, then, is it 41?
I knew I could do it!
```

## showchar1.c

- 作用: 读取一个字符和两个整数，然后将该字符格式化输出为指定行数和列数的矩阵。
- 功能: 
  用户输入一个字符和两个整数，分别表示输出字符以及字符矩阵的行数和列数。程序调用 `display()` 函数按指定格式打印字符阵列。
  （此程序存在逻辑问题：输入后换行符处理不完善可能导致混乱。）

- 演示效果:
```c
/* showchar1.c -- program with a BIG I/O problem */
#include <stdio.h>
void display(char cr, int lines, int width);
int main(void)
{
    int ch;             /* character to be printed    */
    int rows, cols;     /* number of rows and columns */
    printf("Enter a character and two integers:\n");
    while ((ch = getchar()) != '\n')
    {
        scanf("%d %d", &rows, &cols);
        display(ch, rows, cols);
        printf("Enter another character and two integers;\n");
        printf("Enter a newline to quit.\n");
    }
    printf("Bye.\n");
    
    return 0;
}

void display(char cr, int lines, int width)
{
    int row, col;
    
    for (row = 1; row <= lines; row++)
    {
        for (col = 1; col <= width; col++)
            putchar(cr);
        putchar('\n');  /* end line and start a new one */
    }
}
```

```bash
gcc showchar1.c -o showchar1 && echo "@`n2 4`n`n" | ./showchar1
Enter a character and two integers:
@@@@
@@@@
Enter another character and two integers;
Enter a newline to quit.
Bye.
```

## showchar2.c

- 作用: 读取一个字符和两个整数，并按指定行数和列数输出该字符矩阵。
- 功能: 
  用户输入一个字符和两个整数，程序会调用 `display()` 函数按行列格式输出字符阵列。相比 `showchar1.c`，该程序在输入处理上更健壮，能正确跳过多余的输入。

- 演示效果:
```c
/* showchar2.c -- prints characters in rows and columns */
#include <stdio.h>
void display(char cr, int lines, int width);
int main(void)
{
    int ch;             /* character to be printed      */
    int rows, cols;     /* number of rows and columns   */
    
    printf("Enter a character and two integers:\n");
    while ((ch = getchar()) != '\n')
    {
        if (scanf("%d %d",&rows, &cols) != 2)
            break;
        display(ch, rows, cols);
        while (getchar() !=  '\n')
            continue;
        printf("Enter another character and two integers;\n");
        printf("Enter a newline to quit.\n");
    }
    printf("Bye.\n");
    
    return 0;
}

void display(char cr, int lines, int width)
{
    int row, col;
    
    for (row = 1; row <= lines; row++)
    {
        for (col = 1; col <= width; col++)
            putchar(cr);
        putchar('\n');  /* end line and start a new one */
    }
}
```

```bash
gcc showchar2.c -o showchar2 && echo "@`n2 3`n`n" | ./showchar2
Enter a character and two integers:
@@@
@@@
Enter another character and two integers;
Enter a newline to quit.
Bye.
```

## menuette.c

- 作用: 提供一个简单的菜单交互程序，允许用户选择不同功能，如显示建议、发出提示音或进行计数。
- 功能: 
  程序循环显示菜单，用户可选择：
  - a: 显示“买低卖高”的建议文本
  - b: 发出 ANSI 铃声
  - c: 输入一个整数并按顺序从 1 输出到该整数
  - q: 退出程序

- 演示效果:
```c
/* menuette.c -- menu techniques */
#include <stdio.h>
char get_choice(void);
char get_first(void);
int get_int(void);
void count(void);
int main(void)
{
    int choice;
    void count(void);
    
    while ( (choice = get_choice()) != 'q')
    {
        switch (choice)
        {
            case 'a' :  printf("Buy low, sell high.\n");
                break;
            case 'b' :  putchar('\a');  /* ANSI */
                break;
            case 'c' :  count();
                break;
            default  :  printf("Program error!\n");
                break;
        }
    }
    printf("Bye.\n");
    
    return 0;
}

void count(void)
{
    int n,i;
    
    printf("Count how far? Enter an integer:\n");
    n = get_int();
    for (i = 1; i <= n; i++)
        printf("%d\n", i);
    while ( getchar() != '\n')
        continue;
}

char get_choice(void)
{
    int ch;
    
    printf("Enter the letter of your choice:\n");
    printf("a. advice           b. bell\n");
    printf("c. count            q. quit\n");
    ch = get_first();
    while (  (ch < 'a' || ch > 'c') && ch != 'q')
    {
        printf("Please respond with a, b, c, or q.\n");
        ch = get_first();
    }
    
    return ch;
}

char get_first(void)
{
    int ch;
    
    ch = getchar();
    while (getchar() != '\n')
        continue;
    
    return ch;
}

int get_int(void)
{
    int input;
    char ch;
    
    while (scanf("%d", &input) != 1)
    {
        while ((ch = getchar()) != '\n')
            putchar(ch);  // dispose of bad input
        printf(" is not an integer.\nPlease enter an ");
        printf("integer value, such as 25, -178, or 3: ");
    }
    
    return input;
}
```

```bash
gcc menuette.c -o menuette && echo "c`n5`nq" | ./menuette
Enter the letter of your choice:
a. advice           b. bell
c. count            q. quit
Count how far? Enter an integer:
1
2
3
4
5
Enter the letter of your choice:
a. advice           b. bell
c. count            q. quit
Bye.
```

## newguess.c

- 作用: 改进的数字猜测程序，可以处理用户输入非 'y' 或 'n' 的情况并给出提示。
- 功能: 
  与 `guess.c` 类似，通过逐步增加猜测值尝试猜测用户的数字。新增了对非法输入的处理，确保用户必须输入 'y' 或 'n'。

- 演示效果:
```c
/* newguess.c -- an inefficient but improved number-guesser */
#include <stdio.h>
int main(void)
{
    int guess = 1;
    char response;
    
    printf("Pick an integer from 1 to 100. I will try to guess ");
    printf("it.\nRespond with a y if my guess is right and with");
    printf("\nan n if it is wrong.\n");
    printf("Uh...is your number %d?\n", guess);
    while ((response = getchar()) != 'y')     /* get response */
    {
        if (response == 'n')
            printf("Well, then, is it %d?\n", ++guess);
        else
            printf("Sorry, I understand only y or n.\n");
        while (getchar() != '\n')
            continue;                 /* skip rest of input line */
    }
    printf("I knew I could do it!\n");
    
    return 0;
}
```

```bash
gcc newguess.c -o newguess && echo "n`nn`nx`nn`ny" | ./newguess
Pick an integer from 1 to 100. I will try to guess it.
Respond with a y if my guess is right and with
an n if it is wrong.
Uh...is your number 1?
Well, then, is it 2?
Well, then, is it 3?
Sorry, I understand only y or n.
Well, then, is it 4?
I knew I could do it!
```
