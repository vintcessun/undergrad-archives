# 第七课预习报告

## animals.c
- 作用: 演示使用 `switch` 语句根据字母输出对应的动物名称。
- 功能: 程序从标准输入读取一个小写字母，根据字母匹配输出对应的动物名称，遇到 `#` 结束运行。
- 演示效果:
```c
/* animals.c -- uses a switch statement */
#include <stdio.h>
#include <ctype.h>
int main(void)
{
    char ch;
    
    printf("Give me a letter of the alphabet, and I will give ");
    printf("an animal name\nbeginning with that letter.\n");
    printf("Please type in a letter; type # to end my act.\n");
    while ((ch = getchar()) != '#')
    {
        if('\n' == ch)
            continue;
        if (islower(ch))     /* lowercase only          */
            switch (ch)
        {
            case 'a' :
                printf("argali, a wild sheep of Asia\n");
                break;
            case 'b' :
                printf("babirusa, a wild pig of Malay\n");
                break;
            case 'c' :
                printf("coati, racoonlike mammal\n");
                break;
            case 'd' :
                printf("desman, aquatic, molelike critter\n");
                break;
            case 'e' :
                printf("echidna, the spiny anteater\n");
                break;
            case 'f' :
                printf("fisher, brownish marten\n");
                break;
            default :
                printf("That's a stumper!\n");
        }                /* end of switch           */
        else
            printf("I recognize only lowercase letters.\n");
        while (getchar() != '\n')
            continue;      /* skip rest of input line */
        printf("Please type another letter or a #.\n");
    }                        /* while loop end          */
    printf("Bye!\n");
    
    return 0;
}
```
```bash
gcc animals.c -o animals && echo "a`nb`nc`n#`n" | ./animals
Give me a letter of the alphabet, and I will give an animal name
beginning with that letter.
Please type in a letter; type # to end my act.
argali, a wild sheep of Asia
Please type another letter or a #.
babirusa, a wild pig of Malay
Please type another letter or a #.
coati, racoonlike mammal
Please type another letter or a #.
Bye!
```
## break.c
- 作用: 演示使用 `break` 语句在输入条件不满足时退出循环。
- 功能: 程序从标准输入依次读取矩形的长度和宽度，计算并输出面积。当无法读取宽度时（如输入非数字）结束循环。
- 演示效果:
```c
/* break.c -- uses break to exit a loop */
#include <stdio.h>
int main(void)
{
    float length, width;
    
    printf("Enter the length of the rectangle:\n");
    while (scanf("%f", &length) == 1)
    {
        printf("Length = %0.2f:\n", length);
        printf("Enter its width:\n");
        if (scanf("%f", &width) != 1)
            break;
        printf("Width = %0.2f:\n", width);
        printf("Area = %0.2f:\n", length * width);
        printf("Enter the length of the rectangle:\n");
    }
    printf("Done.\n");
    
    return 0;
}
```
```bash
gcc break.c -o break && echo "5`n3`n7.5`n2.2`na`n" | ./break
Enter the length of the rectangle:
Length = 5.00:
Enter its width:
Width = 3.00:
Area = 15.00:
Enter the length of the rectangle:
Length = 7.50:
Enter its width:
Width = 2.20:
Area = 16.50:
Enter the length of the rectangle:
Done.
```
## chcount.c
- 作用: 演示逻辑与操作符 `&&` 的使用，过滤特定字符计数。
- 功能: 程序从输入读取字符直到遇到句号 `.` 为止，统计除双引号 `"` 和单引号 `'` 外的字符数量。
- 演示效果:
```c
// chcount.c  -- use the logical AND operator
#include <stdio.h>
#define PERIOD '.'
int main(void)
{
    char ch;
    int charcount = 0;
    
    while ((ch = getchar()) != PERIOD)
    {
        if (ch != '"' && ch != '\'')
            charcount++;
    }
    printf("There are %d non-quote characters.\n", charcount);
 
    return 0;
}
```
```bash
gcc chcount.c -o chcount && echo "He said \"Hello\".`n" | ./chcount
There are 16 non-quote characters.
```
## colddays.c
- 作用: 统计低于冰点（摄氏0度）的天数百分比。
- 功能: 程序从输入读取每天的最低温度，统计总天数以及低于冰点的天数，最后输出百分比。
- 演示效果:
```c
// colddays.c -- finds percentage of days below freezing
#include <stdio.h>
int main(void)
{
    const int FREEZING = 0;
    float temperature;
    int cold_days = 0;
    int all_days = 0;
    
    printf("Enter the list of daily low temperatures.\n");
    printf("Use Celsius, and enter q to quit.\n");
    while (scanf("%f", &temperature) == 1)
    {
        all_days++;
        if (temperature < FREEZING)
            cold_days++;
    }
    if (all_days != 0)
        printf("%d days total: %.1f%% were below freezing.\n",
               all_days, 100.0 * (float) cold_days / all_days);
    if (all_days == 0)
        printf("No data entered!\n");
    
    return 0;
}
```
```bash
gcc colddays.c -o colddays && echo "-5`n3`n0`n-2`nq`n" | ./colddays
Enter the list of daily low temperatures.
Use Celsius, and enter q to quit.
4 days total: 50.0% were below freezing.
```
## cypher1.c
- 作用: 将输入的每个非空格字符 ASCII 编码加 1，空格保持不变。
- 功能: 程序读取一行输入，将空格原样输出，其它字符的 ASCII 值加 1 后输出，最后换行结束。
- 演示效果:
```c
// cypher1.c -- alters input, preserving spaces 
#include <stdio.h>
#define SPACE ' '             // that's quote-space-quote 
int main(void)
{
    char ch;
    
    ch = getchar();           // read a character         
    while (ch != '\n')        // while not end of line    
    {
        if (ch == SPACE)      // leave the space          
            putchar(ch);      // character unchanged      
        else
            putchar(ch + 1);  // change other characters  
        ch = getchar();       // get next character       
    }
    putchar(ch);              // print the newline        
    
    return 0;
}
```
```bash
gcc cypher1.c -o cypher1 && echo "Hello World`n" | ./cypher1
Ifmmp Xpsme
```
## cypher2.c
- 作用: 将输入的字母字符替换为 ASCII 编码加 1 的新字符，其他非字母字符保持原样。
- 功能: 程序逐个读取字符，若是字母则输出其下一个字母（字符码加1），否则原样输出。
- 演示效果:
```c
// cypher2.c -- alters input, preserving non-letters
#include <stdio.h>
#include <ctype.h>            // for isalpha()
int main(void)
{
    char ch;
    
    while ((ch = getchar()) != '\n')
    {
        if (isalpha(ch))      // if a letter,
            putchar(ch + 1);  // display next letter
        else                  // otherwise,
            putchar(ch);      // display as is
    }
    putchar(ch);              // display the newline
    
    return 0;
}
```
```bash
gcc cypher2.c -o cypher2 && echo "Hello 123!`n" | ./cypher2
Ifmmp 123!
```
## divisors.c
- 作用: 检测一个整数并输出它的约数，如果是质数则标记。
- 功能: 程序读取整数并检查它的所有可能的约数，若能整除则输出配对约数，否则标记为质数；支持连续输入直到退出。
- 演示效果:
```c
// divisors.c -- nested ifs display divisors of a number
#include <stdio.h>
#include <stdbool.h>
int main(void)
{
    unsigned long num;          // number to be checked
    unsigned long div;          // potential divisors
    bool isPrime;               // prime flag
    
    printf("Please enter an integer for analysis; ");
    printf("Enter q to quit.\n");
    while (scanf("%lu", &num) == 1)
    {
        for (div = 2, isPrime = true; (div * div) <= num; div++)
        {
            if (num % div == 0)
            {
                if ((div * div) != num)
                    printf("%lu is divisible by %lu and %lu.\n",
                           num, div, num / div);
                else
                    printf("%lu is divisible by %lu.\n",
                           num, div);
                isPrime= false; // number is not prime
            }
        }
        if (isPrime)
            printf("%lu is prime.\n", num);
        printf("Please enter another integer for analysis; ");
        printf("Enter q to quit.\n");
    }
    printf("Bye.\n");
    
    return 0;
}
```
```bash
gcc divisors.c -o divisors && echo "28`n13`nq`n" | ./divisors
Please enter an integer for analysis; Enter q to quit.
28 is divisible by 2 and 14.
28 is divisible by 4 and 7.
Please enter another integer for analysis; Enter q to quit.
13 is prime.
Please enter another integer for analysis; Enter q to quit.
Bye.
```
## electric.c
- 作用: 根据分段电价计算用电费用。
- 功能: 程序获取输入的用电量（千瓦时），根据不同用电区间使用对应电价计算总费用。
- 演示效果:
```c
// electric.c -- calculates electric bill 
#include <stdio.h>
#define RATE1   0.13230       // rate for first 360 kwh      
#define RATE2   0.15040       // rate for next 108 kwh  
#define RATE3   0.30025       // rate for next 252 kwh
#define RATE4   0.34025       // rate for over 720 kwh       
#define BREAK1  360.0         // first breakpoint for rates  
#define BREAK2  468.0         // second breakpoint for rates 
#define BREAK3  720.0         // third breakpoint for rates
#define BASE1   (RATE1 * BREAK1)
// cost for 360 kwh            
#define BASE2  (BASE1 + (RATE2 * (BREAK2 - BREAK1)))
// cost for 468 kwh
#define BASE3   (BASE1 + BASE2 + (RATE3 *(BREAK3 - BREAK2)))
//cost for 720 kwh
int main(void)
{
    double kwh;               // kilowatt-hours used         
    double bill;              // charges                     
    
    printf("Please enter the kwh used.\n");
    scanf("%lf", &kwh);       // %lf for type double         
    if (kwh <= BREAK1)
        bill = RATE1 * kwh;
    else if (kwh <= BREAK2)   // kwh between 360 and 468     
        bill = BASE1 + (RATE2 * (kwh - BREAK1));
    else if (kwh <= BREAK3)   // kwh betweent 468 and 720
        bill = BASE2 + (RATE3 * (kwh - BREAK2));
    else                      // kwh above 680               
        bill = BASE3 + (RATE4 * (kwh - BREAK3));
    printf("The charge for %.1f kwh is $%1.2f.\n", kwh, bill);
    
    return 0;
}
```
```bash
gcc electric.c -o electric && echo "800`n" | ./electric
Please enter the kwh used.
The charge for 800.0 kwh is $214.38.
```
## paint.c
- 作用: 根据待涂面积计算所需油漆桶数，使用条件运算符简化判断。
- 功能: 程序每次读取待涂面积，计算所需油漆桶数（每桶覆盖 350 平方英尺），考虑余数决定是否需要额外一桶，循环直到退出。
- 演示效果:
```c
/* paint.c -- uses conditional operator */
#include <stdio.h>
#define COVERAGE 350       // square feet per paint can
int main(void)
{
    int sq_feet;
    int cans;
    
    printf("Enter number of square feet to be painted:\n");
    while (scanf("%d", &sq_feet) == 1)
    {
        cans = sq_feet / COVERAGE;
        cans += ((sq_feet % COVERAGE == 0)) ? 0 : 1;
        printf("You need %d %s of paint.\n", cans,
               cans == 1 ? "can" : "cans");
        printf("Enter next value (q to quit):\n");
    }
    
    return 0;
}
```
```bash
gcc paint.c -o paint && echo "400`n300`nq`n" | ./paint
Enter number of square feet to be painted:
You need 2 cans of paint.
Enter next value (q to quit):
```
## skippart.c
- 作用: 演示使用 `continue` 跳过循环中的部分逻辑。
- 功能: 程序读取分数并验证其在有效范围 [0,100]，不合法则跳过当前循环，不计入统计；合法分数记录最小值、最大值并计算平均值。
- 演示效果:
```c
/* skippart.c  -- uses continue to skip part of loop */
#include <stdio.h>
int main(void)
{
    const float MIN = 0.0f;
    const float MAX = 100.0f;
    
    float score;
    float total = 0.0f;
    int n = 0;
    float min = MAX;
    float max = MIN;
    
    printf("Enter the first score (q to quit): ");
    while (scanf("%f", &score) == 1)
    {
        if (score < MIN || score > MAX)
        {
            printf("%0.1f is an invalid value. Try again: ",
                   score);
            continue;   // jumps to while loop test condition
        }
        printf("Accepting %0.1f:\n", score);
        min = (score < min)? score: min;
        max = (score > max)? score: max;
        total += score;
        n++;
        printf("Enter next score (q to quit): ");
    }
    if (n > 0)
    {
        printf("Average of %d scores is %0.1f.\n", n, total / n);
        printf("Low = %0.1f, high = %0.1f\n", min, max);
    }
    else
        printf("No valid scores were entered.\n");
    return 0;
}
```
```bash
gcc skippart.c -o skippart && echo "50`n120`n80`n-5`n70`nq`n" | ./skippart
Enter the first score (q to quit): Accepting 50.0:
Enter next score (q to quit): 120.0 is an invalid value. Try again: Accepting 80.0:
Enter next score (q to quit): -5.0 is an invalid value. Try again: Accepting 70.0:
Enter next score (q to quit): Average of 3 scores is 66.7.
Low = 50.0, high = 80.0
```
## vowels.c
- 作用: 统计输入文本中各元音字母（A、E、I、O、U）的出现次数。
- 功能: 程序读取输入直到遇到 `#` 结束，针对大小写元音字母使用多标签 switch 统计出现次数并输出结果。
- 演示效果:
```c
// vowels.c -- uses multiple labels
#include <stdio.h>
int main(void)
{
    char ch;
    int a_ct, e_ct, i_ct, o_ct, u_ct;
    
    a_ct = e_ct = i_ct = o_ct = u_ct = 0;
    
    printf("Enter some text; enter # to quit.\n");
    while ((ch = getchar()) != '#')
    {
        switch (ch)
        {
            case 'a' :
            case 'A' :  a_ct++;
                break;
            case 'e' :
            case 'E' :  e_ct++;
                break;
            case 'i' :
            case 'I' :  i_ct++;
                break;
            case 'o' :
            case 'O' :  o_ct++;
                break;
            case 'u' :
            case 'U' :  u_ct++;
                break;
            default :   break;
        }                    // end of switch
    }                        // while loop end
    printf("number of vowels:   A    E    I    O    U\n");
    printf("                 %4d %4d %4d %4d %4d\n",
           a_ct, e_ct, i_ct, o_ct, u_ct);
    
    return 0;
}
```
```bash
gcc vowels.c -o vowels && echo "Hello World AEIOU aeio#`n" | ./vowels
Enter some text; enter # to quit.
number of vowels:   A    E    I    O    U
                    2    3    2    4    1
```
## wordcnt.c
- 作用: 统计输入文本的字符数、单词数、行数以及未完整结束的行数。
- 功能: 程序逐字符读取输入，识别单词边界与换行符，并在输入以 `|` 符号终止时输出统计结果。
- 演示效果:
```c
// wordcnt.c -- counts characters, words, lines
#include <stdio.h>
#include <ctype.h>         // for isspace()
#include <stdbool.h>       // for bool, true, false
#define STOP '|'
int main(void)
{
    char c;                 // read in character
    char prev;              // previous character read
    long n_chars = 0L;      // number of characters
    int n_lines = 0;        // number of lines
    int n_words = 0;        // number of words
    int p_lines = 0;        // number of partial lines
    bool inword = false;    // == true if c is in a word
    
    printf("Enter text to be analyzed (| to terminate):\n");
    prev = '\n';            // used to identify complete lines
    while ((c = getchar()) != STOP)
    {
        n_chars++;          // count characters
        if (c == '\n')
            n_lines++;      // count lines
        if (!isspace(c) && !inword)
        {
            inword = true;  // starting a new word
            n_words++;      // count word
        }
        if (isspace(c) && inword)
            inword = false; // reached end of word
        prev = c;           // save character value
    }
    
    if (prev != '\n')
        p_lines = 1;
    printf("characters = %ld, words = %d, lines = %d, ",
           n_chars, n_words, n_lines);
    printf("partial lines = %d\n", p_lines);
    
    return 0;
}
```
```bash
gcc wordcnt.c -o wordcnt && echo "Hello world\nThis is C language|\n" | ./wordcnt
Enter text to be analyzed (| to terminate):
characters = 31, words = 5, lines = 0, partial lines = 1
```
```
```
You need 1 can of paint.
Enter next value (q to quit):
```
```
```
```
```
```
```
```
