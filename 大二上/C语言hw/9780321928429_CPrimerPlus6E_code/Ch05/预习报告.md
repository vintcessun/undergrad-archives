# 第五课预习报告

## add_one.c
- 作用: 演示前缀递增和后缀递增操作符的效果
- 功能: 使用 `super++` 和 `++ultra` 常量循环递增两个变量并输出它们的值
- 演示效果:
```c
/* add_one.c -- incrementing: prefix and postfix */
#include <stdio.h>
int main(void)
{
    int ultra = 0, super = 0;
    
    while (super < 5)
    {
        super++;
        ++ultra;
        printf("super = %d, ultra = %d \n", super, ultra);
    }
    
    return 0;
}
```
```bash
super = 1, ultra = 1
super = 2, ultra = 2
super = 3, ultra = 3
super = 4, ultra = 4
super = 5, ultra = 5
```

## addemup.c
- 作用: 演示 C 语言中五种不同类型的语句，包括声明、赋值、循环、函数调用以及返回语句
- 功能: 循环求前 20 个正整数的和并输出
- 演示效果:
```c
/* addemup.c -- five kinds of statements */
#include <stdio.h>
int main(void)                /* finds sum of first 20 integers */
{
    int count, sum;           /* declaration statement          */
    
    count = 0;                /* assignment statement           */
    sum = 0;                  /* ditto                          */
    while (count++ < 20)      /* while                          */
        sum = sum + count;    /*     statement                  */
    printf("sum = %d\n", sum);/* function statement             */
    
    return 0;                 /* return statement               */
}
```
```bash
0 bottles of spring water!
```

## wheat.c
- 作用: 演示指数级增长的概念
- 功能: 模拟国际象棋棋盘上每格小麦数翻倍，并计算总数和占世界小麦产量的比例
- 演示效果:
```c
/* wheat.c -- exponential growth */
#include <stdio.h>
#define SQUARES 64             // squares on a checkerboard
int main(void)
{
    const double CROP = 2E16;  // world wheat production in wheat grains
    double current, total;
    int count = 1;
    
    printf("square     grains       total     ");
    printf("fraction of \n");
    printf("           added        grains    ");
    printf("world total\n");
    total = current = 1.0; /* start with one grain   */
    printf("%4d %13.2e %12.2e %12.2e\n", count, current,
           total, total/CROP);
    while (count < SQUARES)
    {
        count = count + 1;
        current = 2.0 * current;
        /* double grains on next square */
        total = total + current;     /* update total */
        printf("%4d %13.2e %12.2e %12.2e\n", count, current,
               total, total/CROP);
    }
    printf("That's all.\n");
    
    return 0;
}
```
```bash
square     grains       total     fraction of
           added        grains    world total
   1      1.00e+00     1.00e+00     5.00e-17
   2      2.00e+00     3.00e+00     1.50e-16
   3      4.00e+00     7.00e+00     3.50e-16
   4      8.00e+00     1.50e+01     7.50e-16
   5      1.60e+01     3.10e+01  1.55e-15
   6      3.20e+01     6.30e+01     3.15e-15
...（中间省略）
  64      9.22e+18 1.84e+19     9.22e+02
That's all.
```

## squares.c
- 作用: 输出前20个整数及其平方的表格
- 功能: 使用 `while` 循环递增整数并计算其平方值，按格式输出
- 演示效果:
```c
/* squares.c -- produces a table of first 20 squares */
#include <stdio.h>
int main(void)
{
    int num = 1;
    
    while (num < 21)
    {
        printf("%4d %6d\n", num, num * num);
        num = num + 1;
    }
    
    return 0;
}
```
```bash
1      1
   2      4
   3      9
   4     16
   5     25
   6     36
   7     49
   8     64
   9     81
  10    100
  11    121
  12    144
  13    169
  14    196
  15    225
  16    256
  17    289
  18    324
  19    361
  20    400
```

## sizeof.c
- 作用: 演示 `sizeof` 运算符的用法
- 功能: 使用 `sizeof` 获取 `int` 类型的字节大小，并输出结果
- 演示效果:
```c
// sizeof.c -- uses sizeof operator
// uses C99 %z modifier -- try %u or %lu if you lack %zd
#include <stdio.h>
int main(void)
{
    int n = 0;
    size_t intsize;
    
    intsize = sizeof (int);
    printf("n = %d, n has %zd bytes; all ints have %zd bytes.\n",
           n, sizeof n, intsize );
    
    return 0;
}
```
```bash
n = 0, n has 4 bytes; all ints have 4 bytes.
```

## shoes2.c
- 作用: 计算多种鞋码对应的脚长，并以表格形式输出
- 功能: 使用 `while` 循环逐步增加鞋码，计算每个鞋码的脚长
- 演示效果:
```c
/* shoes2.c -- calculates foot lengths for several sizes */
#include <stdio.h>
#define ADJUST 7.31              // one kind of symbolic constant
int main(void)
{
    const double SCALE = 0.333;  // another kind of symbolic constant
    double shoe, foot;
    
    printf("Shoe size (men's)    foot length\n");
    shoe = 3.0;
    while (shoe < 18.5)      /* starting the while loop */
    {                        /* start of block          */
        foot = SCALE * shoe + ADJUST;
        printf("%10.1f %15.2f inches\n", shoe, foot);
        shoe = shoe + 1.0;
    }                        /* end of block            */
    printf("If the shoe fits, wear it.\n");
    
    return 0;
}
```
```bash
Shoe size (men's)    foot length
       3.0            8.31 inches
       4.0            8.64 inches
       5.0            8.97 inches
       6.0            9.31 inches
       7.0            9.64 inches
       8.0            9.97 inches
       9.0           10.31 inches
      10.0           10.64 inches
      11.0           10.97 inches
      12.0           11.31 inches
      13.0           11.64 inches
      14.0           11.97 inches
      15.0           12.30 inches
      16.0           12.64 inches
      17.0           12.97 inches
      18.0           13.30 inches
If the shoe fits, wear it.
```

## shoes1.c
- 作用: 将男鞋尺码转换为英寸长度
- 功能: 使用符号常量和缩放比例常量，计算鞋码对应的脚长
- 演示效果:
```c
/* shoes1.c -- converts a shoe size to inches */
#include <stdio.h>
#define ADJUST 7.31              // one kind of symbolic constant
int main(void)
{
    const double SCALE = 0.333;  // another kind of symbolic constant
    double shoe, foot;
    
    shoe = 9.0;
    foot = SCALE * shoe + ADJUST;
    printf("Shoe size (men's)    foot length\n");
    printf("%10.1f %15.2f inches\n", shoe, foot);
    
    return 0;
}
```
```bash
Shoe size (men's)    foot length
       9.0           10.31 inches
```

## running.c
- 作用: 将公里制比赛的成绩转换为英里配速和平均速度
- 功能: 输入公里距离及跑步用时（分钟和秒），计算对应的英里配速和平均时速
- 演示效果:
```c
// running.c -- A useful program for runners
#include <stdio.h>
const int S_PER_M = 60;         // seconds in a minute
const int S_PER_H = 3600;       // seconds in an hour
const double M_PER_K = 0.62137; // miles in a kilometer
int main(void)
{
    double distk, distm;  // distance run in km and in miles
    double rate;          // average speed in mph
    int min, sec;         // minutes and seconds of running time
    int time;             // running time in seconds only
    double mtime;         // time in seconds for one mile
    int mmin, msec;       // minutes and seconds for one mile
    
    printf("This program converts your time for a metric race\n");
    printf("to a time for running a mile and to your average\n");
    printf("speed in miles per hour.\n");
    printf("Please enter, in kilometers, the distance run.\n");
    scanf("%lf", &distk);  // %lf for type double
    printf("Next enter the time in minutes and seconds.\n");
    printf("Begin by entering the minutes.\n");
    scanf("%d", &min);
    printf("Now enter the seconds.\n");
    scanf("%d", &sec);
    time = S_PER_M * min + sec;    // converts time to pure seconds
    distm = M_PER_K * distk;       // converts kilometers to miles
    rate = distm / time * S_PER_H; // mph
    mtime = (double) time / distm; // time per mile
    mmin = (int) mtime / S_PER_M;  // find whole minutes
    msec = (int) mtime % S_PER_M;  // find remaining seconds
    printf("You ran %1.2f km (%1.2f miles) in %d min, %d sec.\n",
           distk, distm, min, sec);
    printf("That pace corresponds to running a mile in %d min, ",
           mmin);
    printf("%d sec.\nYour average speed was %1.2f mph.\n",msec,
           rate);
    
    return 0;
}
```
```bash
This program converts your time for a metric race
to a time for running a mile and to your average
speed in miles per hour.
Please enter, in kilometers, the distance run.
Next enter the time in minutes and seconds.
Begin by entering the minutes.
Now enter the seconds.
You ran 10.00 km (6.21 miles) in 42 min, 30 sec.
That pace corresponds to running a mile in 6 min, 50 sec.
Your average speed was 8.77 mph.
```

## rules.c
- 作用: 验证运算符优先级在表达式求值中的作用
- 功能: 使用复杂的运算组合计算结果，并将同一结果赋值给两个变量
- 演示效果:
```c
/* rules.c -- precedence test */
#include <stdio.h>
int main(void)
{
    int top, score;
    
    top = score = -(2 + 5) * 6 + (4 + 3 * (2 + 3));
    printf("top = %d, score = %d\n", top, score);
    
    return 0;
}
```
```bash
top = -23, score = -23
```

## pound.c
- 作用: 定义一个接受整数参数的函数，并根据参数值打印对应数量的字符`#`
- 功能: 通过不同类型的参数（int、char、float）调用同一函数，展示类型转换效果
- 演示效果:
```c
/* pound.c -- defines a function with an argument   */
#include <stdio.h>
void pound(int n);   // ANSI function prototype declaration
int main(void)
{
    int times = 5;
    char ch = '!';   // ASCII code is 33
    float f = 6.0f;
    
    pound(times);    // int argument
    pound(ch);       // same as pound((int)ch);
    pound(f);        // same as pound((int)f);
    
    return 0;
}

void pound(int n)    // ANSI-style function header
{                    // says takes one int argument
    while (n-- > 0)
        printf("#");
    printf("\n");
}
```
```bash
#####
#################################
######
```

## post_pre.c
- 作用: 演示后缀递增（postfix）和前缀递增（prefix）的区别
- 功能: 比较 `a++` 与 `++b` 在赋值阶段的不同效果
- 演示效果:
```c
/* post_pre.c -- postfix vs prefix */
#include <stdio.h>
int main(void)
{
    int a = 1, b = 1;
    int a_post, pre_b;
    
    a_post = a++;  // value of a++ during assignment phase
    pre_b = ++b;   // value of ++b during assignment phase
    printf("a  a_post   b   pre_b \n");
    printf("%1d %5d %5d %5d\n", a, a_post, b, pre_b);
    
    return 0;
}
```
```bash
a  a_post   b   pre_b
2     1     2     2
```

## min_sec.c
- 作用: 将秒数转换为分钟与秒数
- 功能: 输入秒数，程序计算对应的分钟和剩余秒数，直到输入小于等于0结束
- 演示效果:
```c
// min_sec.c -- converts seconds to minutes and seconds
#include <stdio.h>
#define SEC_PER_MIN 60            // seconds in a minute
int main(void)
{
    int sec, min, left;
    
    printf("Convert seconds to minutes and seconds!\n");
    printf("Enter the number of seconds (<=0 to quit):\n");
    scanf("%d", &sec);            // read number of seconds
    while (sec > 0)
    {
        min = sec / SEC_PER_MIN;  // truncated number of minutes
        left = sec % SEC_PER_MIN; // number of seconds left over
        printf("%d seconds is %d minutes, %d seconds.\n", sec,
               min, left);
        printf("Enter next value (<=0 to quit):\n");
        scanf("%d", &sec);
    }
    printf("Done!\n");
    
    return 0;
}
```
```bash
Convert seconds to minutes and seconds!
Enter the number of seconds (<=0 to quit):
125 seconds is 2 minutes, 5 seconds.
Enter next value (<=0 to quit):
70 seconds is 1 minutes, 10 seconds.
Enter next value (<=0 to quit):
Done!
```

## golf.c
- 作用: 演示变量的多重赋值及格式化输出对齐效果
- 功能: 将三个球员分数赋值为相同值并以表格形式显示
- 演示效果:
```c
/* golf.c -- golf tournament scorecard */
#include <stdio.h>
int main(void)
{
    int jane, tarzan, cheeta;
    
    cheeta = tarzan = jane = 68;
    printf("                  cheeta   tarzan    jane\n");
    printf("First round score %4d %8d %8d\n",cheeta,tarzan,jane);
    
    return 0;
}
```
```bash
cheeta   tarzan    jane
First round score   68       68       68
```

## divide.c
- 作用: 演示整数除法、浮点除法以及混合类型除法的差异
- 功能: 通过直接表达式计算并打印不同类型的除法结果
- 演示效果:
```c
/* divide.c -- divisions we have known */
#include <stdio.h>
int main(void)
{
    printf("integer division:  5/4   is %d \n", 5/4);
    printf("integer division:  6/3   is %d \n", 6/3);
    printf("integer division:  7/4   is %d \n", 7/4);
    printf("floating division: 7./4. is %1.2f \n", 7./4.);
    printf("mixed division:    7./4  is %1.2f \n", 7./4);
    
    return 0;
}
```
```bash
integer division:  5/4   is 1
integer division:  6/3   is 2
integer division:  7/4   is 1
floating division: 7./4. is 1.75
mixed division:    7./4  is 1.75
```

## convert.c
- 作用: 演示 C 语言自动类型转换及赋值顺序
- 功能: 展示 `char`、`int`、`float` 类型赋值和运算时的类型提升与截断规则
- 语义错误: 第 15 行将 1107 赋值给 `char`，发生溢出，值从 1107 转换为 83（'S'），虽可运行但会触发编译器警告
- 演示效果:
```c
/* convert.c -- automatic type conversions */
#include <stdio.h>
int main(void)
{
    char ch;
    int i;
    float fl;
    
    fl = i = ch = 'C';                                  /* line 9  */
    printf("ch = %c, i = %d, fl = %2.2f\n", ch, i, fl); /* line 10 */
    ch = ch + 1;                                        /* line 11 */
    i = fl + 2 * ch;                                    /* line 12 */
    fl = 2.0 * ch + i;                                  /* line 13 */
    printf("ch = %c, i = %d, fl = %2.2f\n", ch, i, fl); /* line 14 */
    ch = 1107;                                          /* line 15 */
    printf("Now ch = %c\n", ch);                        /* line 16 */
    ch = 80.89;                                         /* line 17 */
    printf("Now ch = %c\n", ch);                        /* line 18 */
    
    return 0;
}
```
```bash
ch = C, i = 67, fl = 67.00
ch = D, i = 203, fl = 339.00
Now ch = S
Now ch = P
```

## bottles.c
- 作用: 循环演示“100瓶泉水”歌词，每次递减瓶数并输出相应内容
- 功能: 使用 `#define MAX 100` 定义常量，循环输出每一轮的瓶数，并模拟取下一瓶的过程
- 演示效果:
```c
#include <stdio.h>
#define MAX 100
int main(void)
{
    int count = MAX + 1;
    
    while (--count > 0) {
        printf("%d bottles of spring water on the wall, "
               "%d bottles of spring water!\n", count, count);
        printf("Take one down and pass it around,\n");
        printf("%d bottles of spring water!\n\n", count - 1);
    }
    
    return 0;
}
```
```bash
100 bottles of spring water on the wall, 100 bottles of spring water!
Take one down and pass it around,
99 bottles of spring water!

...（中间省略）
1 bottles of spring water on the wall, 1 bottles of spring water!
Take one down and pass it around,
0 bottles of spring water!
```
