# 第十二课预习报告

## diceroll.c
- 作用: 该文件用于实现掷骰子的核心逻辑，包括随机数生成与计数。
- 功能: 通过 `roll_n_dice(int dice, int sides)` 函数，模拟 `dice` 个 `sides` 面的骰子点数总和。
- 演示效果:
```c
/* diceroll.c -- dice role simulation */
/* compile with mandydice.c           */
#include "diceroll.h"
#include <stdio.h>
#include <stdlib.h>           /* for library rand()   */

int roll_count  = 0;          /* external linkage     */

static int rollem(int sides)  /* private to this file */
{
    int roll;
    
    roll = rand() % sides + 1;
    ++roll_count;             /* count function calls */
    
    return roll;
}

int roll_n_dice(int dice, int sides)
{
    int d;
    int total = 0;
    if (sides < 2)
    {
        printf("Need at least 2 sides.\n");
        return -2;
    }
    if (dice < 1)
    {
        printf("Need at least 1 die.\n");
        return -1;
    }
    
    for (d = 0; d < dice; d++)
        total += rollem(sides);
    
    return total;
}
```

```bash
$ gcc manydice.c diceroll.c -o manydice && echo "6`n3`n0" | ./manydice
Enter the number of sides per die, 0 to stop.
How many dice?
You have rolled a 9 using 3 6-sided dice.
How many sides? Enter 0 to stop.
The rollem() function was called 3 times.
GOOD FORTUNE TO YOU!
```

---

## where.c
- 作用: 分析程序中不同类型变量的内存位置，展示静态区、堆区、栈区、常量区的存储分布。
- 功能: 打印各种变量（静态、自动、动态分配、常量字符串）的值与地址，帮助理解内存布局在运行时的差异。
- 演示效果:
```c
//  where.c  -- where's the memory?

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int static_store = 30;
const char * pcg = "String Literal";
int main()
{
    int auto_store = 40;
    char auto_string[] = "Auto char Array";
    int * pi;
    char * pcl;
    
    pi = (int *) malloc(sizeof(int));
    *pi = 35;
    pcl = (char *) malloc(strlen("Dynamic String") + 1);
    strcpy(pcl, "Dynamic String");
    
    printf("static_store: %d at %p\n", static_store, &static_store);
    printf("  auto_store: %d at %p\n", auto_store, &auto_store);
    printf("         *pi: %d at %p\n", *pi, pi);
    printf("  %s at %p\n", pcg, pcg);
    printf(" %s at %p\n", auto_string, auto_string);
    printf("  %s at %p\n", pcl, pcl);
    printf("   %s at %p\n", "Quoted String", "Quoted String");
    free(pi);
    free(pcl);
    
    return 0;
}
```
```bash
$ gcc where.c -o where && ./where
static_store: 30 at 00007ff621279000
  auto_store: 40 at 000000d5c4bff9ec
         *pi: 35 at 000002c026fe9c60
  String Literal at 00007ff62127a000
 Auto char Array at 000000d5c4bff9d0
  Dynamic String at 000002c026fe9c80
   Quoted String at 00007ff62127a06e
```

---

## rand0.c
- 作用: 提供一个基于 ANSI C 标准的伪随机数生成算法（线性同余法）。
- 功能: 该文件的主要函数 `rand0()` 用于生成随机整数，算法依赖静态种子 `next`，实现跨平台的可移植随机数生成。
- 主要错误:  
  编译阶段出现 `undefined reference to WinMain`，原因是文件中没有 `main()` 函数；此文件为函数库文件，应与驱动程序（如 `r_drive0.c`) 联合编译。
- 演示效果:
```c
/* rand0.c –– produces random numbers            */
/*               uses ANSI C portable algorithm  */
static unsigned long int next = 1;  /* the seed  */

unsigned int rand0(void)
{
    /* magic formula to generate pseudorandom number */
    next = next * 1103515245 + 12345;
    return (unsigned int) (next/65536) % 32768;
}
```
```bash
$ gcc rand0.c -o rand0 && ./rand0
undefined reference to WinMain
# 正确方式是：
$ gcc r_drive0.c rand0.c -o r_drive0 && ./r_drive0
16838
5758
10113
17515
31051
```

---

## s_and_r.c
- 作用: 实现自定义随机数生成函数 `rand1()` 以及用于设置种子的函数 `srand1()`。
- 功能: 通过线性同余算法（Linear Congruential Generator, LCG）生成可重复的伪随机数序列。  
  文件不包含 `main()` 函数，仅作为函数库，与 `r_drive1.c` 一起编译和运行。
- 主要错误:  
  单独编译时报错 `undefined reference to WinMain`，因该文件为库函数文件，没有入口函数。
- 演示效果:
```c
/* s_and_r.c -- file for rand1() and srand1()    */
/*                uses ANSI C portable algorithm */
static unsigned long int next = 1;  /* the seed  */

int rand1(void)
{
    /* magic formula to generate pseudorandom number */
    next = next * 1103515245 + 12345;
    return (unsigned int) (next/65536) % 32768;
}

void srand1(unsigned int seed)
{
    next = seed;
}
```
```bash
$ gcc s_and_r.c -o s_and_r && ./s_and_r
undefined reference to WinMain
# 正确方式应为：
$ gcc r_drive1.c s_and_r.c -o r_drive1 && echo "4`n7`nq" | ./r_drive1
Please enter your choice for seed.
1817
24166
10491
3711
15407
Please enter next seed (q to quit):
19564
9806
10868
22674
32531
Please enter next seed (q to quit):
Done
```

---

## r_drive0.c
- 作用: 用于测试 `rand0()` 伪随机数生成函数的输出。
- 功能: 调用 `rand0()` 5 次并打印其返回的随机整数，用以验证随机数生成算法的效果。
- 演示效果:
```c
/* r_drive0.c -- test the rand0() function */
/* compile with rand0.c                    */
#include <stdio.h>
extern unsigned int rand0(void);

int main(void)
{
    int count;
    
    for (count = 0; count < 5; count++)
        printf("%d\n", rand0());
    
    return 0;
}
```
```bash
$ gcc r_drive0.c rand0.c -o r_drive0 && ./r_drive0
16838
5758
10113
17515
31051
```

---

## r_drive1.c
- 作用: 测试改进版随机数生成器 `rand1()` 与种子设置函数 `srand1()` 的行为。
- 功能: 用户输入种子值，程序利用种子初始化并生成一系列伪随机数，演示种子如何影响随机数序列。
- 演示效果:
```c
/* r_drive1.c -- test rand1() and srand1() */
/* compile with s_and_r.c                  */
#include <stdio.h>
#include <stdlib.h>
extern void srand1(unsigned int x);
extern int rand1(void);

int main(void)
{
    int count;
    unsigned seed;
    
    printf("Please enter your choice for seed.\n");
    while (scanf("%u", &seed) == 1)
    {
        srand1(seed);    /* reset seed */
        for (count = 0; count < 5; count++)
            printf("%d\n", rand1());
        printf("Please enter next seed (q to quit):\n");
    }
    printf("Done\n");
    
    return 0;
}
```
```bash
$ gcc r_drive1.c s_and_r.c -o r_drive1 && echo "4`n7`nq" | ./r_drive1
Please enter your choice for seed.
1817
24166
10491
3711
15407
Please enter next seed (q to quit):
19564
9806
10868
22674
32531
Please enter next seed (q to quit):
Done
```

---

## parta.c
- 作用: 演示 C 语言中不同存储类别变量（自动变量、寄存器变量和外部变量）的使用与作用范围。
- 功能: 与 `partb.c` 联合编译，通过输入整数触发循环以展示各类变量的存储特性。外部变量 `count` 用于记录循环执行次数。
- 演示效果:
```c
// parta.c --- various storage classes
// compile with partb.c
#include <stdio.h>
void report_count();
void accumulate(int k);
int count = 0;       // file scope, external linkage

int main(void)
{
    int value;       // automatic variable
    register int i;  // register variable
    
    printf("Enter a positive integer (0 to quit): ");
    while (scanf("%d", &value) == 1 && value > 0)
    {
        ++count;     // use file scope variable
        for (i = value; i >= 0; i--)
            accumulate(i);
        printf("Enter a positive integer (0 to quit): ");
    }
    report_count();
    
    return 0;
}

void report_count()
{
    printf("Loop executed %d times\n", count);
}
```
```bash
$ gcc parta.c partb.c -o partab && echo "3`n5`n0" | ./partab
Enter a positive integer (0 to quit): loop cycle: 1
subtotal: 6; total: 6
Enter a positive integer (0 to quit): loop cycle: 2
subtotal: 15; total: 21
Enter a positive integer (0 to quit): Loop executed 2 times
```

---

## partb.c
- 作用: 与 `parta.c` 配合，展示不同存储类别与链接属性的交互作用。
- 功能: 通过静态变量与外部变量的协作实现部分累计与总和记录；体现函数内部静态变量 `subtotal` 和文件内部静态变量 `total` 的独立性。
- 演示效果:
```c
// partb.c -- rest of the program
// compile with parta.c
#include <stdio.h>

extern int count;       // reference declaration, external linkage

static int total = 0;   // static definition, internal linkage
void accumulate(int k); // prototype

void accumulate(int k)  // k has block scope, no linkage
{
    static int subtotal = 0;  // static, no linkage
    
    if (k <= 0)
    {
        printf("loop cycle: %d\n", count);
        printf("subtotal: %d; total: %d\n", subtotal, total);
        subtotal = 0;
    }
    else
    {
        subtotal += k;
        total += k;
    }
}
```
```bash
$ gcc parta.c partb.c -o partab && echo "3`n5`n0" | ./partab
Enter a positive integer (0 to quit): loop cycle: 1
subtotal: 6; total: 6
Enter a positive integer (0 to quit): loop cycle: 2
subtotal: 15; total: 21
Enter a positive integer (0 to quit): Loop executed 2 times
```

---

## diceroll.h
- 作用: 定义与 `diceroll.c` 交互的外部接口。
- 功能: 声明了外部变量 `roll_count` 及函数 `roll_n_dice(int dice, int sides)`，供主程序调用。
- 演示效果:
```c
//diceroll.h
extern int roll_count;

int roll_n_dice(int dice, int sides);
```
```bash
# 头文件无需单独编译运行
```

---

## dyn_arr.c
- 作用: 演示如何动态分配数组内存并存储用户输入的 double 类型数据。
- 功能: 使用 `malloc()` 分配指定大小的 double 数组，读取用户输入并输出所有数据，最后释放内存。
- 演示效果:
```c
/* dyn_arr.c -- dynamically allocated array */
#include <stdio.h>
#include <stdlib.h> /* for malloc(), free() */

int main(void)
{
    double * ptd;
    int max;
    int number;
    int i = 0;
    
    puts("What is the maximum number of type double entries?");
    if (scanf("%d", &max) != 1)
    {
        puts("Number not correctly entered -- bye.");
        exit(EXIT_FAILURE);
    }
    ptd = (double *) malloc(max * sizeof (double));
    if (ptd == NULL)
    {
        puts("Memory allocation failed. Goodbye.");
        exit(EXIT_FAILURE);
    }
    /* ptd now points to an array of max elements */
    puts("Enter the values (q to quit):");
    while (i < max && scanf("%lf", &ptd[i]) == 1)
        ++i;
    printf("Here are your %d entries:\n", number = i);
    for (i = 0; i < number; i++)
    {
        printf("%7.2f ", ptd[i]);
        if (i % 7 == 6)
            putchar('\n');
    }
    if (i % 7 != 0)
        putchar('\n');
    puts("Done.");
    free(ptd);
    
    return 0;
}
```
```bash
$ gcc dyn_arr.c -o dyn_arr && echo "5`n1.1`n2.2`n3.3`n4.4`n5.5`nq" | ./dyn_arr
What is the maximum number of type double entries?
Enter the values (q to quit):
Here are your 5 entries:
   1.10    2.20    3.30    4.40    5.50
Done.
```

---

## forc99.c
- 作用: 演示 C99 新的代码块作用域规则，尤其是 for 循环中变量声明的作用域。
- 功能: 展示同名变量在不同作用域中的地址和值变化，帮助理解变量遮蔽与作用域生命周期。
- 演示效果:
```c
// forc99.c -- new C99 block rules
#include <stdio.h>
int main()
{
    int n = 8;
    
    printf("   Initially, n = %d at %p\n", n, &n);
    for (int n = 1; n < 3; n++)
        printf("      loop 1: n = %d at %p\n", n, &n);
    printf("After loop 1, n = %d at %p\n", n, &n);
    for (int n = 1; n < 3; n++)
    {
        printf(" loop 2 index n = %d at %p\n", n, &n);
        int n = 6;
        printf("      loop 2: n = %d at %p\n", n, &n);
        n++;
    }
    printf("After loop 2, n = %d at %p\n", n, &n);
    
    return 0;
}
```
```bash
$ gcc forc99.c -o forc99 && ./forc99
Initially, n = 8 at 000000be407ffdac
      loop 1: n = 1 at 000000be407ffda8
      loop 1: n = 2 at 000000be407ffda8
After loop 1, n = 8 at 000000be407ffdac
 loop 2 index n = 1 at 000000be407ffda4
      loop 2: n = 6 at 000000be407ffda0
 loop 2 index n = 2 at 000000be407ffda4
      loop 2: n = 6 at 000000be407ffda0
After loop 2, n = 8 at 000000be407ffdac
```

---

## global.c
- 作用: 演示外部变量（`extern`）的声明与作用范围。
- 功能: 定义了全局变量 `units`，并通过两个函数共享访问，实现输入验证的交互。
- 演示效果:
```c
/* global.c  -- uses an external variable */
#include <stdio.h>
int units = 0;         /* an external variable      */
void critic(void);
int main(void)
{
    extern int units;  /* an optional redeclaration */
    
    printf("How many pounds to a firkin of butter?\n");
    scanf("%d", &units);
    while ( units != 56)
        critic();
    printf("You must have looked it up!\n");
    
    return 0;
}

void critic(void)
{
    /* optional redeclaration omitted */
    printf("No luck, my friend. Try again.\n");
    scanf("%d", &units);
}
```
```bash
$ gcc global.c -o global && echo "12`n56" | ./global
How many pounds to a firkin of butter?
No luck, my friend. Try again.
You must have looked it up!
```

---

## hiding.c
- 作用: 演示变量在不同代码块中的作用域与遮蔽现象。
- 功能: 展示同名变量在外部块、内部块及循环体中的地址和值变化，帮助理解作用域规则。
- 演示效果:
```c
// hiding.c -- variables in blocks
#include <stdio.h>
int main()
{
    int x = 30;      // original x
    
    printf("x in outer block: %d at %p\n", x, &x);
    {
        int x = 77;  // new x, hides first x
        printf("x in inner block: %d at %p\n", x, &x);
    }
    printf("x in outer block: %d at %p\n", x, &x);
    while (x++ < 33) // original x
    {
        int x = 100; // new x, hides first x 
        x++;
        printf("x in while loop: %d at %p\n", x, &x);
    }
    printf("x in outer block: %d at %p\n", x, &x);

    return 0;
}
```
```bash
$ gcc hiding.c -o hiding && ./hiding
x in outer block: 30 at 00000039ce1ffa1c
x in inner block: 77 at 00000039ce1ffa18
x in outer block: 30 at 00000039ce1ffa1c
x in while loop: 101 at 00000039ce1ffa14
x in whileloop: 101 at 00000039ce1ffa14
x in while loop: 101 at 00000039ce1ffa14
x in outerblock: 34 at 00000039ce1ffa1c
```

---

## loc_stat.c
- 作用: 展示局部静态变量与普通局部变量在函数调用间的差别。
- 功能: 重复调用函数 `trystat()`，比较 `fade`（普通局部变量）和 `stay`（静态局部变量）随调用次数的变化情况。
- 演示效果:
```c
/* loc_stat.c -- using a local static variable */
#include <stdio.h>
void trystat(void);

int main(void)
{
    int count;
    
    for (count = 1; count <= 3; count++)
    {
        printf("Here comes iteration %d:\n", count);
        trystat();
    }
    
    return 0;
}

void trystat(void)
{
    int fade = 1;
    static int stay = 1;
    
    printf("fade = %d and stay = %d\n", fade++, stay++);
}
```
```bash
$ gcc loc_stat.c -o loc_stat && ./loc_stat
Here comes iteration 1:
fade = 1 and stay = 1
Here comes iteration 2:
fade = 1 and stay = 2
Here comes iteration 3:
fade = 1 and stay = 3
```

---

## manydice.c
- 作用: 演示如何使用 `diceroll.c` 提供的函数进行多次掷骰子模拟。
- 功能: 接收用户输入的骰子面数和数量，调用 `roll_n_dice()` 计算总点数，并统计函数调用次数。
- 演示效果:
```c
/* manydice.c -- multiple dice rolls                    */
/* compile with diceroll.c                              */
#include <stdio.h>
#include <stdlib.h>              /* for library srand() */
#include <time.h>                /* for time()          */
#include "diceroll.h"            /* for roll_n_dice()   */
/* and for roll_count  */
int main(void)
{
    int dice,roll;
    int sides;
    int status;
    
    srand((unsigned int) time(0)); /* randomize seed      */
    printf("Enter the number of sides per die, 0 to stop.\n");
    while (scanf("%d", &sides) == 1 && sides > 0 )
    {
        printf("How many dice?\n");
        if ((status =scanf("%d", &dice)) != 1)
        {
            if (status == EOF)
                break;             /* exit loop           */
            else
            {
                printf("You should have entered an integer.");
                printf(" Let's begin again.\n");
                while (getchar() != '\n')
                    continue;     /* dispose of bad input */
                printf("How many sides? Enter 0 to stop.\n");
                continue;         /* new loop cycle       */
             }
        }
        roll = roll_n_dice(dice, sides);
        printf("You have rolled a %d using %d %d-sided dice.\n",
               roll, dice, sides);
        printf("How many sides? Enter 0 to stop.\n");
    }
    printf("The rollem() function was called %d times.\n",
           roll_count);           /* use extern variable */
    
    printf("GOOD FORTUNE TO YOU!\n");
    
    return 0;
}
```
```bash
$ gcc manydice.c diceroll.c -o manydice && echo "6`n3`n0" | ./manydice
Enter the number of sides per die, 0 to stop.
How many dice?
You have rolled a 7 using 3 6-sided dice.
How many sides? Enter 0 to stop.
The rollem() function was called 3 times.
GOOD FORTUNE TO YOU!
