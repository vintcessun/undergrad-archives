# 第十课预习报告

## ptr_ops.c
- 作用: 演示各种指针运算（加法、减法、自增自减）及其与数组间的关系。
- 功能:
  1. 定义整型数组 `urn[5]`，并通过多个指针变量操作数组；
  2. 展示取地址、解引用、指针加减整数的效果；
  3. 对两个指针做差，说明结果表示数组元素间的距离；
  4. 理解自增、自减对指针指向内存位置的影响；
  5. 注意其中输出格式字符串有手误：`ptr4` 应为 `ptr1`。
- 语义错误:
  - 在 `printf("ptr1 + 4 = %p, *(ptr4 + 3) = %d\n", ...)` 语句中，`ptr4` 未定义，应改为 `ptr1`。
- 演示效果:
```c
// ptr_ops.c -- pointer operations
#include <stdio.h>
int main(void)
{
    int urn[5] = {100,200,300,400,500};
    int * ptr1, * ptr2, *ptr3;
    
    ptr1 = urn;         // assign an address to a pointer
    ptr2 = &urn[2];     // ditto
    // dereference a pointer and take
    // the address of a pointer
    printf("pointer value, dereferenced pointer, pointer address:\n");
    printf("ptr1 = %p, *ptr1 =%d, &ptr1 = %p\n",
           ptr1, *ptr1, &ptr1);
    
    // pointer addition
    ptr3 = ptr1 + 4;
    printf("\nadding an int to a pointer:\n");
    printf("ptr1 + 4 = %p, *(ptr1 + 3) = %d\n", // 修正 ptr4 为 ptr1
           ptr1 + 4, *(ptr1 + 3));
    ptr1++;            // increment a pointer
    printf("\nvalues after ptr1++:\n");
    printf("ptr1 = %p, *ptr1 =%d, &ptr1 = %p\n",
           ptr1, *ptr1, &ptr1);
    ptr2--;            // decrement a pointer
    printf("\nvalues after --ptr2:\n");
    printf("ptr2 = %p, *ptr2 = %d, &ptr2 = %p\n",
           ptr2, *ptr2, &ptr2);
    --ptr1;            // restore to original value
    ++ptr2;            // restore to original value
    printf("\nPointers reset to original values:\n");
    printf("ptr1 = %p, ptr2 = %p\n", ptr1, ptr2);
    // subtract one pointer from another
    printf("\nsubtracting one pointer from another:\n");
    printf("ptr2 = %p, ptr1 = %p, ptr2 - ptr1 = %td\n",
           ptr2, ptr1, ptr2 - ptr1);
    // subtract an integer from a pointer
    printf("\nsubtracting an int from a pointer:\n");
    printf("ptr3 = %p, ptr3 - 2 = %p\n",
           ptr3,  ptr3 - 2);
    
    return 0;
}
```
```bash
pointer value, dereferenced pointer, pointer address:
ptr1 = 000000d5c5bffb60, *ptr1 =100, &ptr1 = 000000d5c5bffb58

adding an int to a pointer:
ptr1 + 4 = 000000d5c5bffb70, *(ptr4 + 3) = 400

values after ptr1++:
ptr1= 000000d5c5bffb64, *ptr1 =200, &ptr1 = 000000d5c5bffb58

values after --ptr2:
ptr2 = 000000d5c5bffb64, *ptr2 = 200, &ptr2 = 000000d5c5bffb50

Pointers reset to original values:
ptr1 = 000000d5c5bffb60, ptr2 = 000000d5c5bffb68

subtracting one pointer from another:
ptr2 = 000000d5c5bffb68, ptr1 = 000000d5c5bffb60, ptr2 - ptr1 = 2

subtracting an int from a pointer:
ptr3 = 000000d5c5bffb70, ptr3 - 2 = 000000d5c5bffb68
```

## pnt_add.c
- 作用: 演示不同数据类型的指针加法规律，展示指针运算与数据类型大小的关系。
- 功能:
  1. 定义 `short` 与 `double` 类型的数组；
  2. 分别声明对应类型的指针并指向数组首地址；
  3. 通过指针加法（`ptr + index`）操作观察地址变化；
  4. 比较不同类型下每次加 `1` 实际偏移的字节数（`sizeof(type)`）。
- 演示效果:
```c
// pnt_add.c -- pointer addition
#include <stdio.h>
#define SIZE 4
int main(void)
{
    short dates [SIZE];
    short * pti;
    short index;
    double bills[SIZE];
    double * ptf;
    
    pti = dates;    // assign address of array to pointer
    ptf = bills;
    printf("%23s %15s\n", "short", "double");
    for (index = 0; index < SIZE; index ++)
        printf("pointers + %d: %10p %10p\n",
               index, pti + index, ptf + index);
    
    return 0;
}
```
```bash
short          double
pointers+ 0:  7973ff6a0  7973ff680
pointers + 1:  7973ff6a2  7973ff688
pointers + 2: 7973ff6a4  7973ff690
pointers + 3:  7973ff6a6  7973ff698
```

## order.c
- 作用: 演示指针运算中的运算符优先级与结合性，说明 `p++`、`++p`、`(*p)++` 的区别。
- 功能:
  1. 定义两个整型数组 `data` 与 `moredata`；
  2. 使用三个指针 `p1`、`p2`、`p3` 分别指向数组元素；
  3. 展示 `*p1++`（先取值再指针递增）、`*++p2`（先指针递增再取值）、`(*p3)++`（先取值再自增该值）的效果；
  4. 输出每一步操作后的指针指向与值变化。
- 演示效果:
```c
/* order.c -- precedence in pointer operations */
#include <stdio.h>
int data[2] = {100, 200};
int moredata[2] = {300, 400};
int main(void)
{
    int * p1, * p2, * p3;
    
    p1 = p2 = data;
    p3 = moredata;
    printf("  *p1 = %d,   *p2 = %d,     *p3 = %d\n",
           *p1     ,   *p2     ,     *p3);
    printf("*p1++ = %d, *++p2 = %d, (*p3)++ = %d\n",
           *p1++     , *++p2     , (*p3)++);
    printf("  *p1 = %d,   *p2 = %d,     *p3 = %d\n",
           *p1     ,   *p2     ,     *p3);
    
    return 0;
}
```
```bash
*p1 = 100,   *p2 = 100,     *p3 = 300
*p1++ = 100, *++p2 = 200, (*p3)++ = 300
  *p1 = 200,   *p2 = 200,     *p3 = 301
```

## no_data.c
- 作用: 演示未初始化的局部数组的默认值情况，说明其内容为不确定的“垃圾值”。
- 功能:
  1. 定义整型数组 `no_data[SIZE]`，未进行初始化；
  2. 使用 `for` 循环输出数组每个元素的值；
  3. 展示局部变量在栈上分配时的初始状态；
  4. 强调未初始化变量的不可预测性。
- 演示效果:
```c
/* no_data.c -- uninitialized array */
#include <stdio.h>
#define SIZE 4
int main(void)
{
    int no_data[SIZE];  /* uninitialized array */
    int i;
    
    printf("%2s%14s\n",
           "i", "no_data[i]");
    for (i = 0; i < SIZE; i++)
        printf("%2d%14d\n", i, no_data[i]);
    
    return 0;
}
```
```bash
i    no_data[i]
 0    1356386368
 1        32758
 2    1356386376
 3        32758
```

## flc.c
- 作用: 演示复合字面量（compound literals）的使用方法，展示如何在表达式中创建匿名数组。
- 功能:
  1. 使用 `(int [2]){10, 20}` 生成匿名一维数组；
  2. 使用 `(int [2][COLS]){{1,2,3,-9}, {4,5,6,-8}}` 生成匿名二维数组；
  3. 调用函数 `sum()` 与 `sum2d()` 分别对一维、二维数组计算总和；
  4. 展示静态定义与复合字面量的灵活性。
- 演示效果:
```c
// flc.c -- funny-looking constants
#include <stdio.h>
#define COLS 4
int sum2d(const int ar[][COLS], int rows);
int sum(const int ar[], int n);
int main(void)
{
    int total1, total2, total3;
    int * pt1;
    int (*pt2)[COLS];
    
    pt1 = (int [2]) {10, 20};
    pt2 = (int [2][COLS]) { {1,2,3,-9}, {4,5,6,-8} };
    
    total1 = sum(pt1, 2);
    total2 = sum2d(pt2, 2);
    total3 = sum((int []){4,4,4,5,5,5}, 6);
    printf("total1 = %d\n", total1);
    printf("total2 = %d\n", total2);
    printf("total3 = %d\n", total3);
    
    return 0;
}

int sum(const int ar[], int n)
{
    int i;
    int total = 0;
    
    for( i = 0; i < n; i++)
        total += ar[i];
    
    return total;
}

int sum2d(const int ar[][COLS], int rows)
{
    int r;
    int c;
    int tot = 0;
    
    for (r = 0; r < rows; r++)
        for (c = 0; c < COLS; c++)
            tot += ar[r][c];
    
    return tot;
}
```
```bash
total1 = 30
total2 = 4
total3 = 27
```

## designate.c
- 作用: 演示使用 C99 的“指定初始化器”（designated initializers）为数组特定位置赋值。
- 功能:
  1. 定义整型数组 `days[MONTHS]` 并在初始化列表中使用 `[index] = value` 语法；
  2. 同时混合普通初始化与指定初始化器；
  3. 输出数组中每个月的天数，观察未显式赋值的元素是否为 0；
  4. 展示指定初始化器的灵活性与覆盖行为。
- 演示效果:
```c
// designate.c -- use designated initializers
#include <stdio.h>
#define MONTHS 12
int main(void)
{
    int days[MONTHS] = {31,28, [4] = 31,30,31, [1] = 29};
    int i;
    
    for (i = 0; i < MONTHS; i++)
        printf("%2d  %d\n", i + 1, days[i]);
    
    return 0;
}
```
```bash
 1  31
 2  29
 3   0
 4   0
 5  31
 6  30
 7  31
 8   0
 9   0
10   0
11   0
12   0
```

## day_mon3.c
- 作用: 演示使用指针表达式访问数组元素，说明 `*(days + index)` 与 `days[index]` 的等价性。
- 功能:
  1. 定义整型数组 `days[MONTHS]` 存储每个月的天数；
  2. 使用指针偏移法 `*(days + index)` 访问数组元素；
  3. 输出每个月的天数以验证指针与数组的对应关系；
  4. 强调数组与指针在 C 语言中的本质兼容性。
- 演示效果:
```c
/* day_mon3.c -- uses pointer notation */
#include <stdio.h>
#define MONTHS 12

int main(void)
{
    int days[MONTHS] = {31,28,31,30,31,30,31,31,30,31,30,31};
    int index;
    
    for (index = 0; index < MONTHS; index++)
        printf("Month %2d has %d days.\n", index +1,
               *(days + index));   // same as days[index]
    
    return 0;
}
```
```bash
Month  1 has 31 days.
Month  2 has 28 days.
Month  3 has 31 days.
Month  4 has 30 days.
Month  5 has 31 days.
Month  6 has 30 days.
Month  7 has 31 days.
Month  8 has 31 days.
Month  9 has 30 days.
Month 10 has 31 days.
Month 11 has 30 days.
Month 12 has 31 days.
```

## day_mon2.c
- 作用: 演示利用 `sizeof` 运算符自动计算数组元素个数，避免手动定义常量。
- 功能:
  1. 定义常量整型数组 `days[]` 存储每个月的天数；
  2. 使用 `sizeof days / sizeof days[0]` 计算数组长度；
  3. 遍历数组并输出每个月的天数；
  4. 展示 `sizeof` 在数组长度计算中的应用。
- 演示效果:
```c
/* day_mon2.c -- letting the compiler count elements */
#include <stdio.h>
int main(void)
{
    const int days[] = {31,28,31,30,31,30,31,31,30,31};
    int index;
    
    for (index = 0; index < sizeof days / sizeof days[0]; index++)
        printf("Month %2d has %d days.\n", index +1,
               days[index]);
    
    return 0;
}
```
```bash
Month  1 has 31 days.
Month  2 has 28 days.
Month  3 has 31 days.
Month  4 has 30 days.
Month  5 has 31 days.
Month  6 has 30 days.
Month  7 has 31 days.
Month  8 has 31 days.
Month  9 has 30 days.
Month 10 has 31 days.
```

## day_mon1.c
- 作用: 演示使用数组存储每个月的天数并依次输出。
- 功能:
  1. 定义整型数组 `days[MONTHS]` 存储 12 个月的天数；
  2. 使用 `for` 循环遍历数组并输出每个月的天数；
  3. 展示数组初始化与遍历的基本用法。
- 演示效果:
```c
/* day_mon1.c -- prints the days for each month */
#include <stdio.h>
#define MONTHS 12

int main(void)
{
    int days[MONTHS] = {31,28,31,30,31,30,31,31,30,31,30,31};
    int index;
    
    for (index = 0; index < MONTHS; index++)
        printf("Month %d has %2d days.\n", index +1,
               days[index]);
    
    return 0;
}
```
```bash
Month 1 has 31 days.
Month 2 has 28 days.
Month 3 has 31 days.
Month 4 has 30 days.
Month 5 has 31 days.
Month 6 has 30 days.
Month 7 has 31 days.
Month 8 has 31 days.
Month 9 has 30 days.
Month 10 has 31 days.
Month 11 has 30 days.
Month 12 has 31 days.
```

## bounds.c
- 作用: 演示数组越界访问的危险性，说明 C 语言对下标范围不进行检查。
- 功能:
  1. 定义数组 `arr[SIZE]` 并有意访问边界外的元素；
  2. 通过循环索引 -1 到 SIZE，展示数组越界赋值行为；
  3. 输出越界访问导致的意外内存改动；
  4. 显示数组元素与其他变量的地址，说明内存相邻布局。
- 主要错误:
  - 循环访问 `arr[-1]` 与 `arr[SIZE]` 属于越界操作，会造成意外内存覆盖。
- 演示效果:
```c
// bounds.c -- exceed the bounds of an array
#include <stdio.h>
#define SIZE 4
int main(void)
{
    int value1 = 44;
    int arr[SIZE];
    int value2 = 88;
    int i;
    
    printf("value1 = %d, value2 = %d\n", value1, value2);
    for (i = -1; i <= SIZE; i++)
        arr[i] = 2 * i + 1;
    
    for (i = -1; i < 7; i++)
        printf("%2d  %d\n", i , arr[i]);
    printf("value1 = %d, value2 = %d\n", value1, value2);
    
    printf("address of arr[-1]: %p\n", &arr[-1]);
    printf("address of arr[4]:  %p\n", &arr[4]);
    printf("address of value1:  %p\n", &value1);
    printf("address of value2:  %p\n", &value2);
   
    return 0;
}
```
```bash
value1 = 44, value2 = 88
-1  -1
 0  1
 1  3
 2  5
 3  7
 4  9
 5  567
 6  44
value1 = 44, value2 = -1
address of arr[-1]: 0000003e537ff74c
address of arr[4]:  0000003e537ff760
address of value1:  0000003e537ff768
address of value2:  0000003e537ff74c
```

## array2d.c
- 作用: 演示二维数组作为函数参数时的不同传递与访问方式。
- 功能:
  1. 定义 3×4 的二维数组 `junk`；
  2. 调用函数 `sum_rows()`、`sum_cols()` 计算行和列的总和；
  3. 调用函数 `sum2d()` 计算所有元素的总和；
  4. 展示二维数组参数的三种函数声明语法差异。
- 演示效果:
```c
// array2d.c -- functions for 2d arrays
#include <stdio.h>
#define ROWS 3
#define COLS 4
void sum_rows(int ar[][COLS], int rows);
void sum_cols(int [][COLS], int );    // ok to omit names
int sum2d(int (*ar)[COLS], int rows); // another syntax
int main(void)
{
    int junk[ROWS][COLS] = {
        {2,4,6,8},
        {3,5,7,9},
        {12,10,8,6}
    };
    
    sum_rows(junk, ROWS);
    sum_cols(junk, ROWS);
    printf("Sum of all elements = %d\n", sum2d(junk, ROWS));
    
    return 0;
}

void sum_rows(int ar[][COLS], int rows)
{
    int r;
    int c;
    int tot;
    
    for (r = 0; r < rows; r++)
    {
        tot = 0;
        for (c = 0; c < COLS; c++)
            tot += ar[r][c];
        printf("row %d: sum = %d\n", r, tot);
    }
}

void sum_cols(int ar[][COLS], int rows)
{
    int r;
    int c;
    int tot;
    
    for (c = 0; c < COLS; c++)
    {
        tot = 0;
        for (r = 0; r < rows; r++)
            tot += ar[r][c];
        printf("col %d: sum = %d\n", c, tot);
    }
}

int sum2d(int ar[][COLS], int rows)
{
    int r;
    int c;
    int tot = 0;
    
    for (r = 0; r < rows; r++)
        for (c = 0; c < COLS; c++)
            tot += ar[r][c];
    
    return tot;
}
```
```bash
row 0: sum = 20
row 1: sum = 24
row 2: sum = 36
col 0: sum = 17
col 1: sum = 19
col 2: sum = 21
col 3: sum = 23
Sum of all elements = 80
```

## arf.c
- 作用: 演示通过函数操作数组，体现数组作为参数传递时的特性。
- 功能:
  1. 定义数组 `dip[SIZE]` 并输出初始值；
  2. 调用函数 `mult_array()` 对每个元素乘以一个倍率；
  3. 使用 `show_array()` 输出调用前后的数组内容；
  4. 演示函数间数组传递与指针运算的效果。
- 演示效果:
```c
/* arf.c -- array functions */
#include <stdio.h>
#define SIZE 5
void show_array(const double ar[], int n);
void mult_array(double ar[], int n, double mult);
int main(void)
{
    double dip[SIZE] = {20.0, 17.66, 8.2, 15.3, 22.22};
    
    printf("The original dip array:\n");
    show_array(dip, SIZE);
    mult_array(dip, SIZE, 2.5);
    printf("The dip array after calling mult_array():\n");
    show_array(dip, SIZE);
    
    return 0;
}

/* displays array contents */
void show_array(const double ar[], int n)
{
    int i;
    
    for (i = 0; i < n; i++)
        printf("%8.3f ", ar[i]);
    putchar('\n');
}

/* multiplies each array member by the same multiplier */
void mult_array(double ar[], int n, double mult)
{
    int i;
    
    for (i = 0; i < n; i++)
        ar[i] *= mult;
}
```
```bash
The original dip array:
  20.000   17.660    8.200   15.300   22.220
The dip array after calling mult_array():
  50.000   44.150   20.500   38.250   55.550
```

## zippo2.c
- 作用: 演示通过指针变量访问二维数组的方式，与直接使用数组名的效果对比。
- 功能:
  1. 定义指向含两个元素数组的指针 `int (*pz)[2]`；
  2. 让 `pz` 指向二维数组 `zippo`；
  3. 展示 `pz`、`pz[0]`、`*pz`、`pz+1` 等不同层级解引用的关系；
  4. 比较间接访问 `(*(*(pz+2)+1))` 与 `pz[2][1]` 的等价性。
- 演示效果:
```c
/* zippo2.c --  zippo info via a pointer variable */
#include <stdio.h>
int main(void)
{
    int zippo[4][2] = { {2,4}, {6,8}, {1,3}, {5, 7} };
    int (*pz)[2];
    pz = zippo;
    
    printf("   pz = %p,    pz + 1 = %p\n",
           pz,         pz + 1);
    printf("pz[0] = %p, pz[0] + 1 = %p\n",
           pz[0],      pz[0] + 1);
    printf("  *pz = %p,   *pz + 1 = %p\n",
           *pz,        *pz + 1);
    printf("pz[0][0] = %d\n", pz[0][0]);
    printf("  *pz[0] = %d\n", *pz[0]);
    printf("    **pz = %d\n", **pz);
    printf("      pz[2][1] = %d\n", pz[2][1]);
    printf("*(*(pz+2) + 1) = %d\n", *(*(pz+2) + 1));
    
    return 0;
}
```
```bash
pz = 00000004257ffd20,    pz + 1 = 00000004257ffd28
pz[0] = 00000004257ffd20, pz[0] + 1 = 00000004257ffd24
*pz = 00000004257ffd20,   *pz + 1 = 00000004257ffd24
pz[0][0] = 2
  *pz[0] = 2
    **pz = 2
      pz[2][1] = 3
*(*(pz+2) + 1) = 3
```

## zippo1.c
- 作用: 演示二维数组在内存中的地址特性、指针运算规律及不同层级的解引用效果。
- 功能:
  1. 展示二维数组 `zippo[4][2]` 的存储结构；
  2. 比较 `zippo`、`zippo[0]`、`*zippo` 等指针表达式的结果；
  3. 通过递增指针及解引用验证地址偏移规律；
  4. 输出多层次指针访问结果及对应数值。
- 演示效果:
```c
/* zippo1.c --  zippo info */
#include <stdio.h>
int main(void)
{
    int zippo[4][2] = { {2,4}, {6,8}, {1,3}, {5, 7} };
    
    printf("   zippo = %p,    zippo + 1 = %p\n",
           zippo,         zippo + 1);
    printf("zippo[0] = %p, zippo[0] + 1 = %p\n",
           zippo[0],      zippo[0] + 1);
    printf("  *zippo = %p,   *zippo + 1 = %p\n",
           *zippo,        *zippo + 1);
    printf("zippo[0][0] = %d\n", zippo[0][0]);
    printf("  *zippo[0] = %d\n", *zippo[0]);
    printf("    **zippo = %d\n", **zippo);
    printf("      zippo[2][1] = %d\n", zippo[2][1]);
    printf("*(*(zippo+2) + 1) = %d\n", *(*(zippo+2) + 1));
    
    return 0;
}
```
```bash
zippo = 0000004d9a5ffdd0,    zippo + 1 = 0000004d9a5ffdd8
zippo[0] = 0000004d9a5ffdd0, zippo[0] + 1 = 0000004d9a5ffdd4
  *zippo = 0000004d9a5ffdd0,   *zippo + 1 = 0000004d9a5ffdd4
zippo[0][0] = 2
  *zippo[0] = 2
    **zippo = 2
      zippo[2][1] = 3
*(*(zippo+2) + 1) = 3
```

## vararr2d.c
- 作用: 演示变长数组（VLA, Variable Length Array）的定义与使用，展示其在函数形参中的应用。
- 功能:
  1. 使用不同大小的二维数组（固定与变长）；
  2. 利用函数 `sum2d()` 接收变长二维数组参数；
  3. 演示编译器如何通过传递行列数实现灵活维度计算；
  4. 输出不同尺寸矩阵的元素和。
- 演示效果:
```c
//vararr2d.c -- functions using VLAs
#include <stdio.h>
#define ROWS 3
#define COLS 4
int sum2d(int rows, int cols, int ar[rows][cols]);
int main(void)
{
    int i, j;
    int rs = 3;
    int cs = 10;
    int junk[ROWS][COLS] = {
        {2,4,6,8},
        {3,5,7,9},
        {12,10,8,6}
    };
    
    int morejunk[ROWS-1][COLS+2] = {
        {20,30,40,50,60,70},
        {5,6,7,8,9,10}
    };
    
    int varr[rs][cs];  // VLA
    
    for (i = 0; i < rs; i++)
        for (j = 0; j < cs; j++)
            varr[i][j] = i * j + j;
    
    printf("3x5 array\n");
    printf("Sum of all elements = %d\n",
           sum2d(ROWS, COLS, junk));
    
    printf("2x6 array\n");
    printf("Sum of all elements = %d\n",
           sum2d(ROWS-1, COLS+2, morejunk));
    
    printf("3x10 VLA\n");
    printf("Sum of all elements = %d\n",
           sum2d(rs, cs, varr));
    
    return 0;
}

// function with a VLA parameter
int sum2d(int rows, int cols, int ar[rows][cols])
{
    int r;
    int c;
    int tot = 0;
    
    for (r = 0; r < rows; r++)
        for (c = 0; c < cols; c++)
            tot += ar[r][c];
    
    return tot;
}
```
```bash
3x5 array
Sum of all elements = 80
2x6 array
Sum of all elements = 315
3x10 VLA
Sum of all elements = 270
```

## sum_arr2.c
- 作用: 演示使用指针运算遍历数组并求和的过程，体现 C 语言中指针与数组的紧密关系。
- 功能:
  1. 定义整型数组 `marbles[]` 并初始化；
  2. 在 `sump()` 函数中使用两个指针参数表示数组的起始和结束位置；
  3. 通过 `while (start < end)` 循环遍历数组元素并累加；
  4. 展示指针递增与解引用操作。
- 演示效果:
```c
/* sum_arr2.c -- sums the elements of an array */
#include <stdio.h>
#define SIZE 10
int sump(int * start, int * end);
int main(void)
{
    int marbles[SIZE] = {20,10,5,39,4,16,19,26,31,20};
    long answer;
    
    answer = sump(marbles, marbles + SIZE);
    printf("The total number of marbles is %ld.\n", answer);
    
    return 0;
}

/* use pointer arithmetic   */
int sump(int * start, int * end)
{
    int total = 0;
    
    while (start < end)
    {
        total += *start; // add value to total
        start++;         // advance pointer to next element
    }
    
    return total;
}
```
```bash
The total number of marbles is 190.
```

## sum_arr1.c
- 作用: 演示在函数中传递数组时，形参退化为指针的特性，比较 `sizeof` 运算符在不同作用域中的表现。
- 功能:
  1. 主函数中定义整型数组 `marbles[]`，含 10 个元素；
  2. 在 `sum()` 函数中统计数组元素总和；
  3. 比较在主函数与子函数中使用 `sizeof` 的返回值；
  4. 展示数组传参时 C 语言的“指针退化”机制。
- 语义错误:
  - 使用 `sizeof ar` 在函数中获取数组大小是不正确的，因为在函数形参处 `ar` 被视为指针，返回的是指针大小而非数组长度。
- 演示效果:
```c
// sum_arr1.c -- sums the elements of an array
// use %u or %lu if %zd doesn't work
#include <stdio.h>
#define SIZE 10
int sum(int ar[], int n);
int main(void)
{
    int marbles[SIZE] = {20,10,5,39,4,16,19,26,31,20};
    long answer;
    
    answer = sum(marbles, SIZE);
    printf("The total number of marbles is %ld.\n", answer);
    printf("The size of marbles is %zd bytes.\n",
           sizeof marbles);
    
    return 0;
}

int sum(int ar[], int n)     // how big an array?
{
    int i;
    int total = 0;
    
    for( i = 0; i < n; i++)
        total += ar[i];
    printf("The size of ar is %zd bytes.\n", sizeof ar);
    
    return total;
}
```
```bash
sum_arr1.c: In function 'sum':
sum_arr1.c:26:53: warning: 'sizeof' on array function parameter 'ar' will return size of 'int *' [-Wsizeof-array-argument]
   26 |     printf("The size of ar is %zd bytes.\n", sizeof ar);
      |^~
sum_arr1.c:19:13: note: declared here
   19 | int sum(int ar[], int n)     // how big an array?
      |~~~~^~~~
The size of ar is 8 bytes.
The total number of marbles is 190.
The size of marbles is 40 bytes.
```

## some_data.c
- 作用: 演示部分初始化数组的行为，说明未显式初始化的元素会被自动赋值为 0。
- 功能:
  1. 定义数组 `some_data[4]`，仅给前两项赋值；
  2. 输出每个元素的值，观察剩余未赋值项是否为 0；
  3. 展示 C 语言数组初始化规则。
- 演示效果:
```c
/* some_data.c -- partially initialized array */
#include <stdio.h>
#define SIZE 4
int main(void)
{
    int some_data[SIZE] = {1492, 1066};
    int i;
    
    printf("%2s%14s\n",
           "i", "some_data[i]");
    for (i = 0; i < SIZE; i++)
        printf("%2d%14d\n", i, some_data[i]);
    
    return 0;
}
```
```bash
i  some_data[i]
 0          1492
 1          1066
 2             0
 3             0
```

## rain.c
- 作用: 计算多年度降雨量总和、平均值及每月平均降雨量，展示二维数组的实际应用。
- 功能:
  1. 定义 5 年 (2010–2014) 的 12 个月降雨量数组；
  2. 逐年计算年度总降雨量并求平均；
  3. 计算每月跨年平均降雨量；
  4. 将结果按格式输出。
- 演示效果:
```c
/* rain.c  -- finds yearly totals, yearly average, and monthly
 average for several years of rainfall data */
#include <stdio.h>
#define MONTHS 12    // number of months in a year
#define YEARS   5    // number of years of data
int main(void)
{
    // initializing rainfall data for 2010 - 2014
    const float rain[YEARS][MONTHS] =
    {
        {4.3,4.3,4.3,3.0,2.0,1.2,0.2,0.2,0.4,2.4,3.5,6.6},
        {8.5,8.2,1.2,1.6,2.4,0.0,5.2,0.9,0.3,0.9,1.4,7.3},
        {9.1,8.5,6.7,4.3,2.1,0.8,0.2,0.2,1.1,2.3,6.1,8.4},
        {7.2,9.9,8.4,3.3,1.2,0.8,0.4,0.0,0.6,1.7,4.3,6.2},
        {7.6,5.6,3.8,2.8,3.8,0.2,0.0,0.0,0.0,1.3,2.6,5.2}
    };
    int year, month;
    float subtot, total;
    
    printf(" YEAR    RAINFALL  (inches)\n");
    for (year = 0, total = 0; year < YEARS; year++)
    {             // for each year, sum rainfall for each month
        for (month = 0, subtot = 0; month < MONTHS; month++)
            subtot += rain[year][month];
        printf("%5d %15.1f\n", 2010 + year, subtot);
        total += subtot; // total for all years
    }
    printf("\nThe yearly average is %.1f inches.\n\n",
           total/YEARS);
    printf("MONTHLY AVERAGES:\n\n");
    printf(" Jan  Feb  Mar  Apr  May  Jun  Jul  Aug  Sep  Oct ");
    printf(" Nov  Dec\n");
    
    for (month = 0; month < MONTHS; month++)
    {             // for each month, sum rainfall over years
        for (year = 0, subtot =0; year < YEARS; year++)
            subtot += rain[year][month];
        printf("%4.1f ", subtot/YEARS);
    }
    printf("\n");
    
    return 0;
}
```
```bash
YEAR    RAINFALL  (inches)
 2010            32.4
 2011            37.9
 2012            49.8
 2013            44.0
 2014            32.9

The yearly average is 39.4 inches.

MONTHLY AVERAGES:

 Jan  Feb  Mar  Apr  May  Jun  Jul  Aug  Sep  Oct  Nov  Dec
 7.3  7.3  4.9  3.0  2.3  0.6  1.2  0.3  0.5  1.7  3.6  6.7
